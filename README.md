# ESPIRAL

is a simple modular renderer

# Pipeline
the pipeline follows this order:
* local to world -> your logic -> render

to add shaders, place them in:
* local to world -> your logic -> **your shader here** -> render

# Dependencies

This project uses third-party libraries:
* **SDL3** distributed under the zlib license